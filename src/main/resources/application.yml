spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shopper
    username: test_user
    password: Reds-2252!
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
  flyway:
    baseline-on-migrate: true
    user: test_user
    password: Reds-2252!
    schemas: shopper
server:
  port: 8090